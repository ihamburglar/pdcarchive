services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: pdcarchive
      POSTGRES_PASSWORD: pdcarchive
      POSTGRES_DB: pdcarchive
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
